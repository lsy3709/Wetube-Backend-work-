# pytest 설정 – ch05 폴더에서 pytest 실행 시 app 패키지를 찾을 수 있도록 함
[pytest]
pythonpath = .
testpaths = tests
# Flask-Login, Flask-SQLAlchemy 내부 deprecated 경고 무시 (라이브러리 코드)
filterwarnings =
    ignore::DeprecationWarning:flask_login*
    ignore::sqlalchemy.exc.LegacyAPIWarning:flask_sqlalchemy*
